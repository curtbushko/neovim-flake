{
  plugins = {
    cmp-buffer = {
      enable = true;
    };

    cmp-cmdline = {
      enable = true;
    };

    cmp-git = {
      enable = true;
    };

    cmp-emoji = {
      enable = true;
    };

    cmp_luasnip = {
      enable = true;
    };

    cmp-nvim-lsp = {
      enable = true;
    };

    cmp-path = {
      enable = true;
    };

    cmp = {
      enable = true;
    };

    cmp-treesitter = {
      enable = true;
    };
  };
}
